spring:

  batch:
    job:
      enabled: false

  shell:
    interactive:
      enabled: false

  datasource:
    url: jdbc:h2:mem:hw14
    initialization-mode: always
    data: data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true

  data:
    mongodb:
      #uri: mongodb://localhost
      #port: 27017
      port: 0
      database: hw14